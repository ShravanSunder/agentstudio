private class EagerDerivedAtomNonSendableRequest {
    var value = 0
}

@MainActor
private func buildEagerDerivedAtomWithNonSendableRequest() {
    _ = EagerDerivedAtom<EagerDerivedAtomNonSendableRequest, Int, Int>(
        requestIdentity: { $0.value },
        combinePendingRequests: { _, latestRequest in latestRequest },
        isValueEqual: ==,
        project: { $0.value }
    )
}

# Plans

Per-slice implementation plans (`yyyy-mm-dd-<slice>.md`), written just-in-time
from the reviewed design set. Plans contain implementation mechanics only —
tasks, files, commands, threshold numbers, proof runs — never design.

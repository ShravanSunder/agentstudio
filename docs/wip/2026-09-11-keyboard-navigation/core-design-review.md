# Sidebar core design review and parent correction

Scope: U1/U3/U4/U5/U12/U14/U16, Specification R-S1–R-S8/R-S10. U15/R-S9 remains in the full goal and is not ready. Arrangement U6/U7 stays under its existing reviewed committed-focus path; detached-drawer invariant is owner-deferred.

First independent core round: fresh mode-complete Astra reviewer and fresh Sol dispel both completed read-only with full target coverage. Neither ran tests, edited documents or spawned agents. No executable core proof was claimed. No second review or recovery used.

CORE-1 accepted: R-S4 requires selected destination continuity through regrouping. RowID embeds groupID (RepoExplorerRowIdentity.swift), so RowID-only removal fallback could select another target when the original merely moved groups. Dispel classified the correction required by U5/R-S4 and found no over-delivery in the bounded core.

Parent correction is Program Design only: the existing detached update-plan owner first remaps a prior row through pane/worktree destination identity, then applies actual-removal fallback. Surviving exact row wins; otherwise first representation of the same destination in new order. Successor/predecessor fallback also translates surviving destinations before choosing. Associated/unassociated pane transitions share paneUUID identity. Host retains its one local selectedRowID; native RowID and persistence contracts are unchanged.

Parent verified the source identity definitions, grouping construction, current row lookup indexes and native fallback policy against the corrected Navigation metadata section. The correction satisfies the original finding without new stores/atoms/events/coordinators or an owner policy decision. The key map now describes destination rather than group-dependent row continuity.

Result: core design ready for bounded implementation planning with this independent finding source plus parent-verified correction. Whole goal is not ready: preview cold-content restoration and renderer/geometry/cancellation realization remain open; core implementation, automated/native/performance proof and PR gates remain to do. The existing-system structure is within the owner's authorization to finish clear independent scope.

Evidence: tmp/sidebar-keyboard-design/core-design-review-packet.md and core-design-review-findings.md; native agent receipts /root/sidebar_core_design_review and /root/sidebar_core_design_dispel. Normative homes remain Requirements, Specification and Program Design, not this review record.

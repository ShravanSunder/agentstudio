# IPC-v2 coordination for keyboard commands

Recipient discovered through Router: Plan agent IPC v2 implementation in
agent-studio.ipc-improvements, session 01a09b01-f5a8-7770-9fb3-f444d63f1b9c.
Message was accepted and the peer replied. Discovery succeeds outside the sandbox
through the same endpoint; no service restart or alternate transport was used.

Peer confirmed the current exhaustive adaptation: seven semantic AppCommand
identities, no command arguments, pane targets, existing interactive exposure and
terminalInputWrite privilege. Fractions remain runtime policy, not IPC arguments.
No terminal.scroll endpoint or free-form fractional action should be added.

V2 keeps AppCommand.ipcSpec -> typed command.execute -> dispatcher -> existing
execution owner. Its schema/registry/client/auth replacement belongs to the peer.
Expected overlaps: IPC projection, dispatcher/execution owner, PaneTabViewController
and AppPolicies. Keep our changes to identity rows and readable metadata assertions.
Peer explicitly requested keeping the current fingerprint gate valid; root updated
it only after inspecting the intentional Focus Sidebar/scroll metadata delta.

Final handoff owed: exact committed SHA/base or PR, changed files, seven identities
and fractions/directions/eligibility, Core scrollPageUp -> scrollPageFractional API
delta and callers/tests, and actual focused/runtime/native proof. Attribute
focusSidebar separately. No dirty-branch pulling or cross-checkout edits agreed.

Completion boundary: PaneTabViewController starts a Task and returns Bool; this is
not a completed terminal effect. The peer will handle truthful v2 outcome integration.
Our native proof must demonstrate actual effects rather than infer them from Bool.

Latest owner revision sent through the same Router endpoint: scrollSmallStepUp/Down replaces unpublished quarter identities, fraction 0.33, revised seven-key map, Option-Shift-I/K unassigned. Receipt ipc-v2-terminal-map-revision-receipt.json reports steerAccepted for turn 01a09b12-a23d-76e3-9eab-2d8e6136d303; this is delivery acceptance, not a new peer reply. Final committed handoff remains pending.

Peer replied and confirmed the exact 0.33 small-step identities and 0.9 page actions; v2 will carry seven commands with no arguments, pane target and terminalInputWrite. Dirty branch was not imported. Peer owns Sessions/schema now and will preserve acceptance versus completed-effect distinction during execution cutover.

## Incoming checkpoint during pinned integration

IPC-v2 owner reports typed catalog/parser foundation committed at `9f82825292100d60621e7df179d52050f405f955`, with 97 tests/14 suites accepted locally; ClientCore wire cutover remains in progress. No PR/live v2 completion is claimed and no dirty keyboard source was imported. Owner retains exact 0.33/0.9 identities and requests final committed SHA/PR, changed identity/projection files and real terminal-effect proof. No pull of incomplete IPC branch is needed. Immediate Bool remains acceptance, not completed effect.

Our pending final delta must additionally list `focusPreviousPinnedPane` and `focusNextPinnedPane` separately from terminal navigation and `focusSidebar`. Pinned execution is under proof, not yet committed or handed off.

IPC-v2 peer explicitly acknowledged pinned identities and reports checkpoint `99189ff31`. It accepts interactive/targetless/noargs only as temporary phase-one classification. Final v2 relative navigation will use explicit source pane/window context under its existing no-focus-fallback contract. That typed context remains v2 owner work; this branch does not add new transport arguments. Final committed identity/semantics/effect handoff remains owed.

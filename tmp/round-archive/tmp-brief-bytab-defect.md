OWNER-REPORTED DEFECT (screenshot: tmp-owner-bytab-defect.png in worktree root) — By Tab mode is rendering WORKTREE/REPO rows under the TABS header. The screenshot shows ~40 repo-name rows ("actual", "affine-workers", "agentmemory", ... with "Status unavailable; retrying" worktree status lines) in By Tab after a mass folder registration. Contract item 3: By Tab shows PANES grouped by TAB, tab order, header = tab displayTitle + pane count. Repo/worktree rows must NEVER appear in By Tab (nor All Panes) — panes only (plus the association Ungrouped section for panes).

Fix: find why the By Tab (and check All Panes too) data source includes topology rows when many worktrees exist — likely the projection falls through to worktree grouping for tab-less/empty groups or concatenates a repo list. Red test first: fixture with N registered worktrees and zero panes/tabs → By Tab shows the true empty state (item 13), NOT worktree rows; with tabs present → only tab groups. Then re-verify with a mass-registration fixture (many repos, few panes).

Also add to the final sweep: a mass-registration scenario proving By Tab/All Panes show only pane content while By Repo shows the repos with the scanning banner behaving as it did (that part was correct).

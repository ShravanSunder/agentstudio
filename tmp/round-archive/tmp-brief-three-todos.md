Three owner todos, exact specs — add to your current round; keep working item A/B from the prior message first if unfinished.

TODO 1 — L2 STATUS-FACT SPLIT (owner-diagnosed root cause of "output activity" never updating): the L2 line rides the unseen-activity NOTIFICATION lane, and InboxPromoter suppresses small bursts for the observed/attended pane ("observed_small_activity"), so the pane you're typing in never updates its own L2. Fix by splitting lanes:
  - New runtime-only fact: last output line + timestamp per pane, written at EVERY settle from TerminalSettledActivity.lastOutputLine regardless of notification suppression. Home: a keyed runtime atom slot (follow SessionRuntimeAtom's pattern in Core/State/MainActor/Atoms — runtime lane, NOT persisted, survey-does-not-mean-persist).
  - Row L2 reads this status fact first; falls back to last content-bearing notification body; then "output activity"/"No activity yet".
  - Notification/alerting suppression rules UNTOUCHED.
  - Tests red-first: observed pane + small settle → status fact updated, no notification (suppression preserved), row renders the fresh line.
TODO 2 — FORGE-LANE HONESTY (pending glyph forever): rows whose repos have no GitHub remote, or whose forge queries fail terminally, keep prCount == nil forever → eternal pending glyph. Mirror the #310 principle: every forge outcome reaches a terminal state. No-remote → resolved-no-data (NO glyph, no chip). Failure → backoff, and after the honesty threshold render nothing rather than eternal pending. Pending glyph only while a fetch is genuinely expected. Find the ForgeActor outcome handling; red test: no-remote repo → row renders no PR glyph after resolution.
TODO 3 — PR CHIP ICON PARITY: pane rows (RepoExplorerPaneNavigation chipRow: .octicon("octicon-git-pull-request")) and By Repo worktree rows use DIFFERENT glyphs for the same PR-count fact. Unify: one icon constant for the PR chip everywhere, sourced from one shared spec (follow how the worktree row builds its PR chip; whichever glyph the By Repo row uses is the canonical one). Same size/color/pill.

All three: red-first tests, focused suites + lint green, commit each separately, push (#296 draft), evidence rows in tmp-RESULT.md. Live proof for TODO 1 (type in an observed pane, L2 updates within the settle window — screenshot) and TODO 2 (fixture no-remote repo resolves to no glyph).

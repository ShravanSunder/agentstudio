SUPERSEDE NOTE: the item-5 lastOutputLine seam is being implemented in PARALLEL by another agent on a separate branch (feat/item5-last-output-line) and will be merged into this branch by the orchestrator. When you reach the earlier item-5 briefs (diagnosis brief and seam-implementation brief): SKIP their implementation steps entirely — do only this: remove the synthetic-notification fake evidence and re-mark item 5 honestly as IN-PARALLEL. Continue with all other queued work (toggle burst, animated glyph, launch hygiene). HOLD the MASTER GATE final sweep until the orchestrator confirms the seam merge landed in this branch — the sweep must include real L2 content proof on the combined build. If you reach the master gate before that confirmation arrives, pause and check for a tmp-brief-seam-merged.md file; loop-wait on its existence (check every few minutes via sleep) rather than sweeping early.

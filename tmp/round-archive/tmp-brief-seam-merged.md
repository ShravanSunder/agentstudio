SEAM MERGED — feat/item5-last-output-line merged into feat/sidebar-grouping-rows.

Merge commit: (see `git log -1` on feat/sidebar-grouping-rows immediately after this file was written)
Source commit merged: e37bf41f0 "Surface real terminal output in inbox notification bodies"
Merge base: 7188d484e

Merge was clean (no conflicts) via `git merge feat/item5-last-output-line --no-edit`.

Focused seam suites after merge:
`mise run test:swift -- --filter "TerminalActivityProjectorTests|InboxPromoterTests|TerminalLastOutputLineContractTests"`
Result: 40 tests in 3 suites, exit 0, 96.12s.

Master gate item C (real L2 content) is now provable end-to-end; live capture happens in the item-1 final sweep along with the alignment proof.

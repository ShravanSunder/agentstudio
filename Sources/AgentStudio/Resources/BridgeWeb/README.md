# BridgeWeb Resources

`app/` is generated by `mise run bridge-web-build` from `BridgeWeb/` source.

Do not edit or commit generated files under `app/`; SwiftPM copies this
directory into the app bundle after the build task regenerates it.

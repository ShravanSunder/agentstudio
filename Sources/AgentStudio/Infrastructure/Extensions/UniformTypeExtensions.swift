import UniformTypeIdentifiers

extension UTType {
    /// Type for dragging existing tabs
    package static let agentStudioTab = UTType(exportedAs: "com.agentstudio.tab")

    /// Type for dragging new tab button
    package static let agentStudioNewTab = UTType(exportedAs: "com.agentstudio.newtab")

    /// Type for dragging individual panes
    package static let agentStudioPane = UTType(exportedAs: "com.agentstudio.pane")
}
